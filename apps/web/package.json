{
  "name": "@downloader/web",
  "version": "0.1.0",
  "private": true,
  "description": "Single-page UI: paste URL, pick a variant, watch progress, download.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@downloader/shared": "*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "vite": "^7.2.0"
  }
}
