node_modules/
dist/
build/
*.tsbuildinfo

.env
.env.local
.env.*.local

# Runtime artifacts
storage/
downloads/
tmp/
*.log
logs/

# Local database
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/

# Browser profiles used by the sniffer resolver
.browser-profiles/
playwright-report/
test-results/
# Generated HLS fixtures, the API's storage dir, traces and videos.
**/e2e/.artifacts/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Agents. Worktrees are created in here and live inside the repo, so without
# this every parallel session shows up as untracked clutter in every other one.
# Ignored wholesale rather than by name, because whatever a future tool drops
# beside them is local state too — `settings.json` is the one deliberate
# exception, being the permission allowlist the repo shares.
.claude/*
!.claude/settings.json
