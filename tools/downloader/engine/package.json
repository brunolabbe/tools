{
  "name": "@downloader/engine",
  "version": "0.1.0",
  "private": true,
  "description": "ProbeResult to a file on disk. ffmpeg runner, segment download, mux, storage, retention GC.",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@downloader/contract": "*",
    "ffmpeg-static": "^5.2.0"
  }
}
