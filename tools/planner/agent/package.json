{
  "name": "@planner/agent",
  "version": "0.1.0",
  "private": true,
  "description": "The planning agent: the roster, the specialists, the fan-out over them, and the seam every model provider plugs into.",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@planner/contract": "*",
    "@webtools/core": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@planner/itinerary": "*"
  }
}
