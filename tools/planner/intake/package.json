{
  "name": "@planner/intake",
  "version": "0.1.0",
  "private": true,
  "description": "The authored question tree, what the answers so far open, and what an edit discards. No model, no network, no clock.",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@planner/contract": "*"
  }
}
