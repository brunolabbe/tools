{
  "name": "@planner/itinerary",
  "version": "0.1.0",
  "private": true,
  "description": "Everything that must be exact: season filtering, day packing, budget arithmetic, the constraint check and the critic. No model, no network.",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@planner/contract": "*"
  }
}
